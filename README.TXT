# Lab 2 Submission

## Steps to Execute
1. Open MySQL Workbench and connect to your Local Instance.
2. Run the provided table creation and insert statements (from Activity.pdf).
3. Execute each `.sql` file in order (q1.sql → q5.sql).
4. Use CALL statements to test:
   - CALL ListAllSubscribers();
   - CALL GetWatchHistoryBySubscriber(1);
   - CALL AddSubscriberIfNotExists('New User');
   - CALL SendWatchTimeReport();
   - CALL PrintAllSubscribersHistory();

## Git Repository Link
https://github.com/<your-username>/<repo-name>
